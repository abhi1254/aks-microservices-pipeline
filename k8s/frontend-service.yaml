# frontend-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: ClusterIP   # 👈 pehle LoadBalancer tha
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 3000
