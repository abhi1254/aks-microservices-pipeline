apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8000        # service port
      targetPort: 8000  # container port
  type: ClusterIP
